const username = 'username';
const password = 'usernamepassword'
const defaultTimeout = 2_000
const followTimeout = 125_000;


module.exports = {
    username,
    password,
    defaultTimeout,
    followTimeout
}